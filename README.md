# ProjectCodeMap

プロジェクトのディレクトリ構造をスキャンし、Pythonファイルから関数一覧を抽出するツール。

## インストール方法

```bash
pip install .
```

## 使用方法

```bash
project-code-map
```

## ローカルでの実行

ローカルで実行する場合は以下のコマンドを使用してください。

```bash
run.bat
```
